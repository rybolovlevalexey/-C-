#include <iostream>
using namespace std;
int main() {
  int a, b, c, d, e, f, g;
    cin >> a >> b >> c;
    d = (a + 1) / 2;
    e = (b + 1) / 2;
    f = (c + 1) / 2;
    g = d + e + f;
    cout << g;
  return 0;
}
